name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: curl keep-alive
        run: curl --fail https://edutrust-backend.onrender.com/
